---
description: Show autonomy system status (daemon, tier, workers, queue)
---

Run the autonomy status check:

```bash
~/sixth/packages/aut/bin/aut status
```

Report: daemon UP/DOWN, tier level, cycle count, active workers, pending tasks.
