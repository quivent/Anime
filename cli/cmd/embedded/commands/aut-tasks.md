---
description: List or add autonomy tasks
argument-hint: [add "command"] | [list] | [show <id>]
---

Run task operations via the Sixth autonomy CLI:

```bash
~/sixth/packages/aut/bin/aut tasks $ARGUMENTS
```

If no args, lists pending tasks. Use `add "command"` to enqueue, `show <id>` for details.
