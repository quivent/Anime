Open Safari Web Inspector for the current app.

Use `hermes_webinspector_list_apps` to see available apps, then `hermes_webinspector_open_inspector` with the app name to open the inspector.

Requires: Safari running, target app running, MCP server configured.
